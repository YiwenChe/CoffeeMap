import React, { useRef, useEffect, useState } from 'react';

export default function MainTitle() {
    return (
        <div>
            <h4>First thing first, COFFEE please</h4>
        </div>
    );
}